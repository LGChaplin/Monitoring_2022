# R code for EM 
# Need to install other packages which aren't already in R
# Search for raster package to manage image data and install it
# https://cran.r-project.org/web/packages/raster/index.html

install.packages("raster")
# Need the quote marks to install packages outside of R

# There are functions to use installed packages 
# Packages can be found in libraries 

library(raster)
# Don't need quote marks bc the package has been installed and therefore is in R
